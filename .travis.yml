language: generic
sudo: true

branches:
  only:
    - stable
    - master

matrix:
  include:
  - os: linux
    language: generic
    dist: trusty
  - os: linux
    language: generic
    dist: precice

install:
  - make clean || true
  - make install || true

before_script:
  - make clean || true

script:
  - make init || true
  - make build || true
  - make test || true

after_success:
  - make uninstall || true
  - make clean || true

